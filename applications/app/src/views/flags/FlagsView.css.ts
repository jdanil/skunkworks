import { style } from "@vanilla-extract/css";

export const flagStyle = style({
  alignItems: "center",
  display: "flex",
  marginBottom: "1rem",
  marginTop: "1rem",
});

export const flagIdStyle = style({
  color: "grey",
});

export const flagMetadataStyle = style({
  flex: 1,
});

export const flagNameStyle = style({
  fontWeight: "bold",
});
